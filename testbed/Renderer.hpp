#ifndef JP_RENDERER
#define JP_RENDERER

// TEMPORARY
#include <iostream>

struct Renderer
{
	/* Renderer(); */
	/* ~Renderer(); */

	void Create();
	/* void Destroy(); */
};

extern Renderer renderer;

#endif
